#! /usr/bin/env bash
env=${env:-dev}

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
RESULTS_FILE=/tmp/result.json
ansible-playbook "$SCRIPT_DIR/../playbook/deploy-viewcounter-backend.yml" -e env=$env --ask-vault-pass
